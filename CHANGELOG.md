# 更新日志

## [2.0.0] - 2026-08-02

### 更新：多工程管理与节点编辑器全面重构

#### 新增功能
- **多工程管理**  
  - 支持创建、切换、重命名、删除多个工程。  
  - 每个工程独立保存节点、连线、标签等数据。  
  - 工程数据存储在 localStorage，并支持导出/导入为 JSON 文件。  
  - 新增“主页”视图，集中管理所有工程，支持双击卡片快速切换。

- **节点编辑器增强**  
  - **集线器节点**：新增“集线器”节点类型，用于整理连线，自身不产生代码。  
  - **条件节点**：修复条件节点分支处理，相同条件的多条路径合并为一个 `if` 块。  
  - **参数顺序标准化**：代码生成时，`input`/`output` 参数顺序固定为：数量、`each`、资源ID、保留、方向、槽位。方向与槽位置于 `from/to` 之后。  
  - **自动 `forget`**：在同一个 `every` 块内，不同输入链路之间自动插入 `forget`，避免资源冲突。  
  - **连接线优化**：连接线采用贝塞尔曲线，端口处水平伸出，更美观且避免遮挡节点。

- **交互与体验**  
  - **整体平移**：在画布空白区域拖拽鼠标可整体平移所有节点（无缩放）。  
  - **自动保存**：节点位置、设置、连线等修改后自动保存至 localStorage。  
  - **未保存提醒**：关闭标签页时，若当前工程有未保存变更，会弹出确认提示。  
  - **端口状态管理**：删除节点后，关联端口状态自动重置，不再残留“已连接”样式。

- **工程设置**  
  - 右侧面板新增“工程设置”选项卡，可查看工程节点/连接/标签数量，并支持重命名工程。

#### 修复问题
- 修复节点 ID 类型不匹配导致的连接失败（UUID 与字符串统一）。  
- 修复条件节点分支重复生成多个 `if` 块的问题。  
- 修复集线器节点不被识别为通路的问题。  
- 修复删除节点后端口状态不更新的问题。  
- 修复相同输入分支下重复生成 `input` 语句的问题（现合并为一条）。  
- 修复 `!=` 比较符不被支持的问题（自动转换为 `not ==`）。  
- 修复多工程切换时编辑器内容未正确刷新的问题。  
- 修复 localStorage 数据恢复时节点 ID 丢失的问题。

#### 性能与稳定性
- 全面优化代码生成逻辑，减少冗余语句。  
- 提升大工程（大量节点与连接）的渲染与保存性能。

#### 开发与维护
- 将代码拆分为 17 个独立模块（`01-splitter.js` ~ `17-pan.js`），便于维护与扩展。  
- 提供 `exportDebugJSON` 功能，便于调试与反馈。

---

## [1.0.0] - 2026-07-xx（早期版本）
初始版本，包含基础节点编辑、连线、代码生成、单工程管理。